export type InventoryData = {
  created_at: string;
  id: string;
  product_code: string;
  product_name: string;
  quantity: string;
};

export type formDataDetails = {
  product_code: string;
  product_name: string;
  quantity: string;
};
